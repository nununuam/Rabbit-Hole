<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="public/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300&family=Rajdhani&display=swap" rel="stylesheet">
</head>
<body>
    <div class="navbar">
        <a href="/home">Home</a>
        <a href="/upload">Upload</a>
        <a href="/edit">Edit</a>
        <a href="/browse">Browse</a>
      
       
    </div>

<nav>
   
  
    
    <% videos.forEach ( video => { %>
        <a href="/edit/<%video._id%>">
            <tr>
                <td><form id=<%= video.id %> method="POST" action="/edit"></form></td>
                <td><input type="text" form=<%= video.id %> placeholder="<%= video.title %>" > </td>
                <td><input type="text" form=<%= video.id %> placeholdere="<%= video.categories %>" > </td>
                <td><input type="text" form=<%= video.id %> placeholder="<%= video.links %>" ></td>
                <td><input type="submit" value="Delete" form=<%= video.id %> /></td>
            </tr>
        </a>
       
        <% }) %>


    </nav>
   
    <tr>
        <th></th>
        <th>Title</th>
        <th>Categories</th>
        <th>Links</th>
        <th></th>
    </tr>
    
    <% videos.forEach ( video => { %>
        <tr>
            <td><form id=<%= video.id %> method="POST" action="/edit"></form></td>
            <td><input type="text" form=<%= video.id %> placeholder="<%= video.title %>" > </td>
            <td><input type="text" form=<%= video.id %> readonly value="<%= video.categories %>" > </td>
            <td><input type="text" form=<%= video.id %> readonly value="<%= video.links %>" ></td>
            <td><input type="submit" value="Edit" form=<%= video.id %> /></td>
        </tr>
        <% }) %>

</table>
    
</body>
</html>